const usernames = ['Cammie', 'Chidi', 'Benny', 'Chirag']
const passwords = ['56#hdj', '$56#asd', '$56#mqw', '$56#sla']

function arr_return(usernames, passwords) {

  for (x in usernames) {
    holder = {} 
    holder.x = password
  }

  console.log(holder)
}

var animals = {"rat":0, "Hamster":100, "cat":200, "dog":300, "rabbit":400, "lizard":500, "goldfish":600,"fly":700}

function adder(animal_obj) {
  sum = 0
  for (x in animal_obj) {
    if (x.length == 3) {
      sum += animal_obj[x]
    } else {
      sum -= animal_obj[x]
    }
  }
}

